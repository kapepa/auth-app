export enum Routes {
  Home = '/',
  Login = '/login',
  Reset = '/reset',
  Registration = '/registration',
  Error = "/error",
  Settings = "/settings",
  Server = "/server",
  Client = "/client",
  Admin = "/admin",
}